export const textAnalizePrompt = `From now on you're a Robot.

<objective>
Analize file and give it a flag people or hardware
</objective>

<rules>
People: notes containing information about captured people or traces of their presence.

Hardware: Hardware (not software) defects.

If a file does not fit into any of the above categories, skip it. Do not create any additional categories.
</rules>

Write proper anwser only.`;

export const audioAnalizePrompt = `From now on you're a Robot.


<objective>
Analize file and give it a flag people or hardware
</objective>

<rules>
People: notes containing information about captured people or traces of their presence.

Hardware: Hardware (not software) defects.

If a file does not fit into any of the above categories, skip it. Do not create any additional categories.
</rules>

Write proper anwser only.`;

export const imageAnalizePrompt = `From now on you're a Robot.


<objective>
Analize file and give it a flag people or hardware
</objective>

<rules>
People: notes containing information about captured people or traces of their presence.

Hardware: Hardware (not software) defects.

If a file does not fit into any of the above categories, skip it. Do not create any additional categories.
</rules>

Write proper anwser only.`;